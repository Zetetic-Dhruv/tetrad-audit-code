# Discovery Tetrad — Code Release

Anonymous code release for double-blind review.

## Contents
- Locked specification files: `judge_prompt.txt`, `assembly_mapping.json`,
  `output_schema.json`, `dispatch_map.json`, `SCHEMA.md`.
- Audit harness: `aggregate_stats.py`, `validate_outputs.py`.
- Reproducibility: `REPRODUCE.md`, `judge_protocol_template.txt`,
  `croissant_metadata.json`, `DATASHEET.md`, `ETHICS.md`, `LICENSE`.
- Paper figures: `paper_figures/` (TikZ source).

## Companion dataset
Substrate inputs and per-goal audit outputs are released separately.
See the paper's reproducibility section for the dataset URL.
